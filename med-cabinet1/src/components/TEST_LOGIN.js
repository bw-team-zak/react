import React from "react";

export const TEST = (props) => {
  // console.log(params.id);
  return (
    <div>
      <h1 style={{ fontSize: "500%" }}>THIS IS THE PROTECTED ROUTE</h1>
    </div>
  );
};
